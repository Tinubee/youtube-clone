header
    a(href="/").header__logo
        i.fab.fa-youtube
    nav
        ul
            if isLoggedIn
                li
                    a(href="/videos/upload")
                        i.fas.fa-video
                li
                    a(href="/users/edit")
                        i.fas.fa-user-cog
                li
                    a(href="/users/logout")
                        i.fas.fa-sign-out-alt
                li
                    a(href=`/users/${user._id}`)
                        if !user.avatarUrl
                            span 😀
                        else
                            if user.avatarUrl.includes("k.kakaocdn.net")
                                img.header__avatar(src=user.avatarUrl)
                            else
                                if user.avatarUrl.startsWith("https://") 
                                    img.header__avatar(src=user.avatarUrl,crossorigin)
                                else 
                                    img.header__avatar(src="/"+user.avatarUrl,crossorigin)
            else        
                li
                    a(href="/login")  Login                        
                li
                    a(href="/join").header__btn  Join
                                                       